# zielonki v2

## Revision 68 — mapped tree restoration

On 11 September 2026 the user requested the missing emailed-map trees back in every design. All 17 mapped refs are now present, preserving the surviving plum's data. The existing field outbuilding, house, carport and other deletions are unchanged. `before-site-restoration-r67.json` preserves the preceding publication; immutable baselines remain intact. Main slopes remain 44° / 37.8710376967° at a shared 7.7856938754 m ridge, with the junction recognized. Outbuilding slopes remain symmetric 37° at a 6.2490506628 m ridge. See [capture, merge and verification](../zielonki/README.md#2026-09-11--restore-the-emailed-map-trees-and-share-the-field-outbuilding).

## Revision 67 — joined roof ridges, 11 September 2026

The user requested equal ridge heights, restored roof intersection and removal of the roof fragment inside the house, explicitly for both compact variants. Source: actual user profile at `http://localhost:5173/`, active v2 **r66**, captured approximately 08:03 UTC; full IndexedDB recovery is in ignored `tmp/roof-join-1789113797284/workspace-before.json`. `before-roof-join-r66.json` preserves the preceding publication. The r66 browser matched canonical v2; reference-aware publication synchronization yields saved/live **r68**, without conflicts. Other user edits and deletions remain intact.

Root cause: the facade narrowing changed the two perpendicular roof spans to 6.08 and 7.55 m, but kept the same pitch and a stale branch endpoint. The ridges consequently differed by about 0.620 m and the branch missed the host ridge by 0.75 m, disabling junction clipping. Front-barn is now **44°**, rear-barn **37.8710376967°**, both meeting at model elevation **7.7856938754 m**; eaves remain 4.85 m and knee walls 1.40 m. The rear-barn minimum X moves from -1.705 to the current host ridge X **-0.955 m**. This restores the valley, suppresses its internal gable and cuts the host roof sheet out of the joined volume. Wall/room dimensions, openings, furniture, canopy, other buildings and placement are unchanged. Both main slopes satisfy the cached 37–45° requirement in `AGENTS.md`; model elevation is not statutory height above terrain, and the limitations recorded in `knowledge-bank/zielonki/ZONING.md` remain.

Verification: the regression checks recompute pitches from span/rise, compare ridge elevations, require a recognized valley and test by ray intersection that the internal roof sheet is absent while the exterior sheet remains. Exterior browser rendering shows the joined roof. Fresh/existing Chromium profiles receive r67/r68 and retain an independent rename and car deletion across reload. **402 tests pass** and production build passes. An interrupted full-suite run had timeouts; rerunning with two workers passed all tests. Tests tied to the historical bathroom edit now use its immutable r66 snapshot, while current roof tests check the actual geometry. Code baseline remains `e34fc160` plus local work; no commit, push or deployment for this correction.

The user-requested third project, **Z garażem za domem przy sąsiadach**, is documented separately in [zielonki-rear-carport](../zielonki-rear-carport/README.md). It forks preserved r49; this current v2 and all of its baselines remain unchanged by that addition.

Current tracked data: **project/zielonki-v2, revision 67, “Garaz przód”**, including the shorter/narrower house, enlarged bathroom and storage, the house-facing 99 m² outbuilding concept and the user's removal of two courtyard surfaces. Open **Garaz przód** in Projects. This continues the existing v2 identity in the working-house release. Other designs retain their independent edits. See [deployment verification](../../docs/mikrus-deployment.md) and the [release capture](../zielonki/README.md) for code/data provenance.

## Revision 50 — road-facing carport and rearward house placement

The existing two-car 6.40 × 6.40 m carport moves to the road-facing +z facade. In house coordinates its slab spans x −4.905…1.495 m, z 8.485…14.885 m, meeting the outside wall at z=8.485 m. The carport's own local geometry, furniture and stable refs remain unchanged; its independent building anchor moves. Main-house rotation remains **273.06927082481445°**. Both floors, roof pitch, knee walls and remaining side pergola are unchanged.

The house moves by **(+8.530350793, −3.596624316) m**, to **(1.901446476, −6.653672344) m**. The fit maximizes displacement away from the surveyed road parcel edge at the fixed rotation, subject to all house/carport/slab/roof envelopes staying in mapped MNU with a 0.15 m margin and the house retaining the existing 4 m southern neighbor separation. A 3 m carport-side separation was also retained as a concept constraint. The intersection of the translated allowed polygons gives the maximum road-normal coordinate; the MNU/R line and house neighbor separation are active limits. Physical canopy fascia extends 0.06 m beyond its centreline footprint and is included.

Outside house walls are approximately **12.71 m** from the road parcel edge; the carport fascia is approximately **6.25 m** away (slab: 6.31 m). These distances reference the mapped road parcel, not an independently measured asphalt edge. About **0.15 m** remains to the digitized MNU/R division. Coordinate precision is computational: the map fit is not confirmation of the statutory building line or whole-building compliance. The [plan §6(2)(1)](https://rastry.gison.pl/mpzp-public/zielonki/uchwaly/U_06_2007_55_IX.pdf#page=4), rechecked 2026-09-10, separately requires the building line on the drawing; see [zoning evidence](../../knowledge-bank/zielonki/ZONING.md).

The road approach and user-marked house-road entrance are realigned to the new carport. An L-shaped pedestrian path links the approach, main entrance and utility door. The field entrance, survey boundaries, north, neighbors, garden fixtures and plants remain fixed. The obsolete `zone/zielonki-v2/pergola-paving` front pad left from r46 is deleted to complete the previously requested front-terrace removal; no extra old pad remains at the former building position.

`before-road-carport-r49.json` preserves the complete previous published data, supplementing the immutable r46 baseline. The actual public-origin Codex profile was backed up and exported before editing; its active v2 r49 exactly equalled its own published baseline and tracked r49. The original browser r47 matched published original r46 except revision/date metadata. No independent edits or conflicts required merging. Raw records, including proposals and older recoveries, remain in the ignored recovery directory recorded in `../zielonki/README.md`.

Validation covers the road-facing attachment, complete MNU containment, active rear/side limits, unchanged rooms and roofs, original-project isolation, deletion and independent-edit survival across r49→r50 migration and reload. The browser variants audit now starts the desktop from a saved r49 baseline and checks the house icon, mobile targets and variant switching.

## Revision 49 — remove front terrace and trim the side pergola

The user highlighted the front pergola in `Zrzut ekranu 2026-09-10 184434.png`. That entire front bay (`roof/zielonki-v2/garden-pergola`) and the paving in front of the house are removed. The retained side bay (`roof/reference/courtyard-canopy`) is shortened to the front outside wall face at local z=8.485 m: its 12 cm end beam is centred at z=8.425 m, with slats and end posts moved accordingly. The remaining rectangular terrace has local corners (2.185, 1.095) and (5.595, 8.485), so its paving also ends flush with the wall.

House rotation/position, carport, room geometry, main roof pitch, north, neighbors, fixtures and plants remain unchanged from r48. `before-terrace-trim-r48.json` preserves the previous published project; the original r46 migration baseline is unchanged. The normal entity-based published-data merge preserves independent edits and reports edit-versus-delete conflicts.

The actual public-origin Codex browser was captured before editing in ignored `tmp/v2-terrace-trim-recovery-20260910-185010`. Its full exported records exactly match the r48 pre-deployment capture (original project only, no additional v2 working copy or new edits). This change therefore starts from tracked published v2 r48. Tests verify the physical beam/paving alignment, deletion across reload, and unchanged house/carport geometry. The original house remains published r46.

## Revision 48 — user-requested 180° rotation

On 2026-09-10 the existing v2 was turned 180° as a complete assembly: house, both retained pergolas and carport. Rotation is now **273.06927082481445°**. The anchor moves from (−8.630577683, −6.209756862) to **(−6.628904317, −3.057048028)**: rotating in place would cross MNU and reduce the outside-wall road distance to 2.17 m. The corrected position retains **4.00 m** from outside house faces to the surveyed road parcel edge, with every roof/pergola footprint inside MNU. This is still a conceptual setback, not a verified legal building line.

The carport is now north-northwest, so its visible name no longer says “South”. Its opposite open edge connects to the rebuilt road apron. Terrace and entrance path follow the house. Local room, wall, opening, furniture and roof geometry are identical to r47; 1.40 m knee walls and 40.134234° gables are unchanged. North, neighbors, plants and fixtures remain fixed in survey coordinates. Existing tree deletions remain deletions.

`before-rotation-r47.json` preserves the complete previous published project. It supplements, and does not replace, the immutable first-publication r46 baseline. Existing browser r47 data merges using its own saved published baseline; tests verify an independent room rename and car deletion survive this rotation and reload.

Actual public-origin browser data was captured on 2026-09-10 at 18:37:30 Europe/Warsaw from the Codex profile for `http://natan203.mikrus.xyz:20203/`. Raw profile backup and full exported records are in ignored `tmp/v2-rotation-recovery-20260910-183730/`. This origin contained the original project only and exactly matched the previous pre-deployment capture; no new browser edits or merge conflicts were found. V2 changes therefore use the tracked published r47, not a test profile. The original project remains untouched at published r46.

## Source, recovery and merge — r47 history

The actual Codex browser profile at `http://localhost:5173/` was captured on 2026-09-10 at 18:09:26 Europe/Warsaw, with active `project/zielonki-v2` r46, saved 2026-09-10T15:07:28.021Z. Its data equals the previous tracked v2 exactly. Raw profile plus complete workspace/proposal records are recoverable in ignored `tmp/v2-carport-recovery-20260910-180926/`.

- `project.json` is now v2 r49; `before-rotation-r47.json` preserves the r47 carport and room/placement changes.
- `before-carport-r46.json` is the immutable baseline for first publication of this existing v2 identity. Existing browser v2 projects merge against it by entity ref and field; later publications use their saved published baseline. Revision counters do not settle conflicts. A merged existing browser may display r48 while having the same design content as published r47.
- `before-pergola-r45.json` retains the complete earlier workspace and the common baseline for shared site evidence. The reviewed cadastral neighbor correction from original-house commit `244e264` is merged by ref/field against that baseline; independent v2 data is preserved and divergent fields would stop generation. No conflicts were found in the captured data.
- Conflicting browser placement, edit/delete and same-field changes retain both versions; Projects reports the conflicts. No profile is reset and the original project is not replaced.

## V2 geometry — r47 placement before the rotation

Both existing open pergolas, all upstairs spaces and the stair/slab opening are retained. The shortened building has no enclosed garage extension, garage gate, old car or roof terrace. The main roof segments remain at 40.13423424862029° and the attic knee walls remain 1.40 m.

The revised ground floor follows the supplied screenshot: open living/kitchen/dining and entrance hall, kitchen and stairs beside the carport, bathroom (about 7.18 m² clear), guest bedroom/study (14.71 m²) and utility (9.57 m²). The utility has an exterior entrance and a bathroom connection; the guest bedroom is private. Dimensions are adapted to the retained upper floor, not presented as a measured architectural reproduction. Retained room refs and geometrically unchanged wall refs remain stable; changed topology uses new wall refs.

The kitchen-side carport is south-southeast, with access toward the road. To achieve that orientation the whole house/pergolas are rotated 180° from previous v2, to 93.06927082481445°. Site north, trees, garden fixtures and neighbors retain their survey coordinates. The carport is independently selectable and has a 6.40 × 6.40 m footprint, four 16 cm posts, a graphite roof/timber soffit and two 1.90 × 4.70 m car envelopes in 3.20 m parking modules. The entrance has no middle post, 5.92 m clear width and approximately 2.44 m clearance beneath the fascia. These are visual configurations, not structural specifications or verified vehicle swept paths.

Outside house faces are 4.00 m from the surveyed road parcel edge; the carport slab is approximately 4.10 m from it. House position is x −8.630577683 m, z −6.209756862 m in model coordinates. The carport roof/fascia governs the shift toward the diagonal MNU/R limit: approximately 0.15 m remains inside MNU; the house is approximately 3.35 m away. House, carport and both pergolas remain in mapped 06.MNU.8. The entry path/apron and L-shaped garden terrace move with the design.

**4 m is the requested conceptual setback, not a confirmed legal minimum.** [MPZP IX/55/2007 §6(2)(1)](https://rastry.gison.pl/mpzp-public/zielonki/uchwaly/U_06_2007_55_IX.pdf) requires the line on the plan drawing. Road-side building line (CAD 591A), road category, driveway permission, canopy setbacks, turning paths and whole-building compliance remain unverified. Preserving the main roof pitch does not establish those permissions. See [zoning evidence](../../knowledge-bank/zielonki/ZONING.md).

Five survey trees intersect the carport: `plant/survey-5012`, `plant/survey-5015`, `plant/survey-5018`, `plant/survey-501b`, `plant/apple`. Their deletion is a **proposed removal in v2 only**, disclosed in the chooser and model notes. The original house and r46 recovery baseline preserve their records. `plant/orchard-plum` and all six garden fixtures remain at their original world positions. This visualization is not tree-removal authorization.

## Build and verification

`python scripts/build-carport-study.py --output tmp/carport.json` deterministically reconstructs the historical r47 revision in `before-rotation-r47.json` from the v2 r46 baseline, common r45 baseline and reviewed neighbor data. It does not reproduce the later rotation. Do not use it to overwrite current `project.json` or any historical baseline.

`node scripts/audit-house-studies.mjs --url http://127.0.0.1:5173/` checks desktop/mobile switching, independent saves, reload, modal focus and 44 px targets at 1440×1000, 360×640, 390×844 and 844×390. Screenshots and results are in ignored `output/carport-study/`. Unit tests verify the existing v2 migration, both retained pergolas, room/opening geometry, MNU fit, road distance, true-south side, deletion survival and conflict preservation. Worker tests prevent stale geometry when revisions decrease across projects or undo operations.

## Historical notes: v2 r46 before this publication

The following describes the earlier local-only revision and its recovery evidence, not the current publication state.

Current local project: project/zielonki-v2, revision 46, saved 2026-09-10T15:07:28.021Z. Created as an independent copy of the actual Codex in-app browser workspace at http://localhost:5173/, source project/zielonki-spatial-v2 r45. The original capture matched the tracked source JSON.

## Revision 46 — lighter house with an open pergola

The projecting garage bay was shortened by 3.17 m to the upper-floor facade at local z=8.385 m. The enclosed area beneath the retained bedrooms is now a garden room with glazed garden doors, preserving its stable space ref. The garage gate, car and garage shelving were removed. The ground slab ends at z=8.485 m. All upstairs rooms remain; both former terrace doors are now windows with 1.00 m sills.

The garage roof segment and its terrace/guards were deleted. The courtyard canopy is now a slatted pergola, with a second bay across the former projecting garage footprint. The frames have 12 cm posts and 18 cm beams, with 7 cm timber slats at approximately 45 cm spacing; top elevation is 3.10 m in building coordinates. These are open, non-walkable structures without an opaque roof, fascia skirt or glass guards. Both main gables retain 40.13423424862029 degrees, unchanged eaves/ridges and 1.40 m knee walls. This model is a design concept, not a structural design or confirmation of whole-building planning compliance.

The change uses only the captured v2 r45 as its baseline. No changes were merged into the original project. All retained entity refs, six plants, six garden fixtures, site evidence and other room/furniture data are preserved. During verification a separate canonical-project edit triggered automatic refresh; v2 was reopened and verified without importing that update.

## Recovery and storage

project.json is the current extracted v2 model. before-pergola-r45.json contains the complete previous workspace. The browser also has a recoverable project named "zielonki v2 · przed pergolą" (project/zielonki-v2/before-pergola-r45). The raw capture is in tmp/zielonki-v2-pergola-2026-09-10T15-02-45-869Z/before.json. Original pre-copy backups remain in tmp/zielonki-v2-2026-09-10T14-59-25-899Z/.

The v2 option is saved in IndexedDB at http://localhost:5173/. This data is not bundled as a fresh-profile default or published to another origin. No commit, push, deployment or migration-baseline replacement was performed. Code baseline for this work: 244220544fea63c96de1f2bd029a9385841147d9 plus existing working changes.

## Pergola model and checks

RoofSegmentModel.canopy.slats optionally specifies direction (x or z, the beam span), spacingM and widthM. Only axis-aligned rectangular flat segments with open gaps and no roof terrace are valid. The shared pergolaMembers helper supplies the renderer and sunlight occluders, so light passes through the gaps. Existing solid canopies keep their behavior.

Schema/domain validation and TypeScript checks passed. Tests cover open-versus-solid shade, schema round trips, post/beam connections, and v2 save/load, restore migrations and canonical synchronization without recreating deleted garage elements. Actual-browser reload/open preserved the exact v2 JSON. Both main gables and bedrooms equal the r45 baseline.

## Precision editor release — 2026-09-10

V2 remains r49 without model changes. See ../zielonki/README.md for the actual public-origin recovery comparison; no v2 working copy was present in that captured profile. New controls permit precise editing of this existing variant without creating another project. Canonical geometry, deletions and the immutable before-carport r46 baseline are unchanged.
## Revision 51 — garden outbuilding concept

Added to the existing `project/zielonki-v2`, without changing the house, carport, existing landscape or site evidence:

- `building/garden-outbuilding`, centred at model (−9, 53) m, with 9 × 16 m wall-centre dimensions. The exterior wall face stays approximately 4.78 m inside the left boundary. The existing 8 m field entrance at z=66–74 m feeds the new camper apron; no new public-road access is asserted.
- Camper garage approximately 4.8 × 9.8 m clear, with a 3.6 × 3.7 m opening and a 7.4 × 2.35 × 3.1 m vehicle envelope. Separate workshop, sauna, shower/changing room, WC and garden-facing relaxation room have shared semantic walls and named rooms.
- Graphite standing-seam gable at 40.134234°, timber garden elevation, large glazing and an open timber pergola. The new roof has 0.60 m overhangs; the existing main-house roof and 1.40 m knee walls are unchanged.
- Approximately 111.7 m² terrace, a 3.6 m outdoor kitchen, six-seat dining area, jacuzzi and two loungers. All fixtures have stable refs and remain individually selectable and removable.

**Planning limitation:** this requested location lies on agricultural 06.R.21. [IX/55/2007 §28](https://rastry.gison.pl/mpzp-public/zielonki/uchwaly/U_06_2007_55_IX.pdf#page=27), rechecked on 2026-09-10, prohibits new buildings there. `designStatus: "concept"` permits this visual study to remain saved while retaining a visible `building.site` warning. An ordinary building at this position, or a concept outside the site boundary, still fails validation. Roof shape alone does not establish permission. Vehicle turning, structural design, sauna installation, services and gate operation are conceptual; the generic door-swing check can flag the camper at its wide garage opening. Adjacent stepped sauna benches intentionally trigger the furniture proximity warning.

Source: actual Codex browser **http://localhost:5173/**, active v2 r52; full recovery export in ignored `tmp/outbuilding-recovery-1789064690686/workspace.json`. The working copy matched its saved r50 baseline in content. Three-way merge by ref/field against tracked r50 had no conflicts. `before-outbuilding-r50.json` preserves the previous canonical snapshot; original r46, `before-carport-r46.json` and `before-pergola-r45.json` remain unchanged. This change updates local project data and the localhost browser; it does not deploy to Mikrus or GitHub Pages.

`python scripts/build-outbuilding-study.py --output tmp/outbuilding-study.json` reconstructs this r51 from its preserved r50 source. The generator and browser audit follow `script-quality-gate`; neither reads raw browser credentials. `node scripts/audit-outbuilding.mjs` verifies fresh and existing isolated Chrome workspaces, local edit/deletion survival and reload, writing screenshots and `audit.json` to `output/outbuilding-audit/`. Unit tests cover geometry, retained house data, concept-only planning validation, migration and same-field conflicts. The scene now renders glazing on rectangular authored barns and keeps finished ground surfaces above the planning overlay.

Actual localhost verification: browser r55 contains the same geometry as canonical r51. Five pre-existing numeric values differ only by floating-point roundoff below 2e-15; the original project workspace is exactly unchanged. Full after-export and comparison are in the same ignored recovery directory. Code was checked against `6d972d4` plus these local working changes; TypeScript, production build using Node 24 and 55 focused tests passed. No commit, push or deployment is part of this request.

Final browser audit passed in both fresh and existing isolated Chrome profiles: all three buildings loaded, the existing room rename and car deletion survived, and deleting the jacuzzi stayed effective after save/reload. Results and screenshots are in `output/outbuilding-audit/`. Documentation review covered both project READMEs and the new script contracts; the shared barn-detail predicate and fixture renderer keep the implementation local and avoid duplicate model logic.

## Revision 52 — compact outbuilding perpendicular to the road

The user's correction replaces the large r51 mass with **10.24 × 6.74 m = 69.0176 m²** measured at external walls. This is the enclosed concept footprint; the separate 81.92 m² terrace and open pergola are not included in that figure. It is not a statutory area or permit assessment. The single-storey flat roof is at 3.80 m (approximately 3.9 m overall), matching the graphite/timber vocabulary of the main garage and pergola. Main-house walls, knee walls, gables, carport and original landscape remain unchanged.

The existing building ref now has anchor (−8, 68.5) m and rotation **270°**. Its long axis runs across the field road, with the 3.0 × 3.4 m camper opening facing the left entrance. The garage is approximately 3.20 × 9.76 m clear around the retained 7.4 × 2.35 × 3.1 m camper. A separate workshop, sauna and combined WC/shower/changing room occupy the other strip. The indoor lounge and redundant partitions are deleted; outdoor dining, kitchen, jacuzzi and loungers remain on the relocated terrace. Surviving room, wall, opening, furniture, fixture and roof refs are retained, including the main roof segment ref despite its changed roof type. The agricultural-land concept warning described for r51 remains applicable. The generic hinged-door warning for the camper and the stepped sauna-bench overlap warning remain conceptual limitations.

Source: actual Codex browser **http://localhost:5173/** on 2026-09-10, working v2 r55 against its saved published r51 baseline. Full before/after recovery and merge review: ignored `tmp/compact-outbuilding-recovery-1789066562833/`. Only five pre-existing floating-point differences below 2e-15 and revision/date metadata distinguished the working copy from tracked r51. A three-way ref/field merge found no conflicts; canonical data retain their original precision and browser migration retains the working values. `before-compact-outbuilding-r51.json` preserves the previous complete canonical concept. All earlier recovery snapshots and immutable migration baselines remain unchanged.

`python scripts/compact-outbuilding-study.py` reproduces the r52 proposal in `tmp/compact-outbuilding.json`. The historical r51 generator continues to reproduce r51. Geometry/schema checks, five outbuilding tests, ten house-study tests and TypeScript passed against code `6d972d4` plus local working changes. Tests include r51→r52 migration with independent colour changes and car deletion, and explicit conflict preservation when a moved fixture was locally deleted. The actual browser contains v2 r56 after migration; the original project workspace is exactly unchanged. No commit, push or deployment was requested. Final user-profile preview: `output/compact-outbuilding/overview.png`.

The final `scripts/audit-outbuilding.mjs` run passed in both fresh and existing isolated Chrome profiles. The compact geometry loaded, the prior room rename and car deletion survived migration, and removing the jacuzzi persisted across save/reload. Results and screenshots: `output/compact-outbuilding-audit/`. The audit waits up to 30 seconds for asynchronous persistence; the original five-second assertion had timed out while the deletion was already visible in the UI. The actual user's workspace retains the jacuzzi.

## Revision 53 — facing the house with an enclosed car space

The user requested the opposite orientation, questioned the flat roof, and explicitly chose a car **inside** the garage with permission to exceed 70 m². The same outbuilding now turns from 270° to **90°**, putting its terrace toward the main house while keeping the long axis perpendicular to the field road. Its anchor moves from (−8, 68.5) to (−9, 64.8) m so the 4.5 m extension remains inside the wider plot. External dimensions are **14.74 × 6.74 m = 99.3476 m²**. The terrace remains 81.92 m² and all five outdoor fixtures rotate with it. The garage gate moves to the opposite gable to retain access from the existing left entrance; the workshop entrance moves to the house-facing terrace elevation.

The enclosed garage is approximately 3.20 × 14.26 m clear. A new car envelope of 1.90 × 4.70 m stands ahead of the retained 2.35 × 7.40 m camper, with a 0.40 m gap. **This is tandem parking: the car must leave before the camper can exit.** The enlarged workshop, sauna and sanitary room remain separately defined. Shared wall, room, roof, furniture and fixture refs are retained; `interior/outbuilding/car` is the only new model entity.

The main outbuilding roof becomes a symmetric **37° gable**, with 0.60 m overhangs, 3.80 m eaves and approximately 6.25 m ridge. This corrects the unsupported flat-roof assumption; it does not certify the building or site as compliant. The agricultural 06.R.21 prohibition and concept-only warning remain. See [roof evidence follow-up](../../knowledge-bank/zielonki/ZONING.md#outbuilding-roof-follow-up--10-september-2026). Main-house roofs, attic knee walls and carport are exactly unchanged.

Actual source: **http://localhost:5173/**, working v2 r56 against its published r52 baseline, captured on 2026-09-10 in ignored `tmp/outbuilding-facing-house-1789067262210/workspace.json`. The previous canonical is preserved in `before-house-facing-outbuilding-r52.json`. A three-way entity/field merge found no conflicts; the same five pre-existing roundoff differences remain in the browser. After migration, actual v2 r59 matches canonical r53 apart from those values and revision/date metadata. The original project workspace is exactly unchanged. Recovery, merge review, after-export and comparison remain in that ignored directory; no immutable migration baseline was changed.

`python scripts/face-outbuilding-to-house.py` reproduces r53 from preserved r52. Geometry/schema checks, five outbuilding tests, ten house-study tests and TypeScript passed against code `6d972d4` plus local changes. Tests check the vehicle envelopes/gap, gate side, terrace direction, r52 migration with independent edits/deletions and unresolved-conflict preservation. User-profile visual preview: `output/house-facing-outbuilding/overview.png`. This is a local project update; no commit, push or deployment was requested.

Final browser audit passed in fresh and existing isolated Chrome profiles: the 90° building and 37° gable loaded with both vehicles, the earlier room rename/car deletion survived migration, and jacuzzi removal persisted across save/reload. Results and screenshots are in `output/house-facing-outbuilding-audit/`. The actual user's jacuzzi remains present.

## Revision 54 — clearly inside the wider site

The user requested placement on the wider part of the plot. The complete building/terrace/SPA group moves **10 m toward the house**, from anchor (−9, 64.8) to **(−9, 54.8) m**. The external building outline now spans model z=51.43–58.17 m; the wider site ends at z=69.05 m, leaving more than 10 m even after allowing for the roof overhang. The terrace spans z=43.43–51.43 m. The existing field entrance at z=66–74 m stays fixed, with the access strip extended to meet it. The access connection alone continues back to that entrance; the building, terrace and workshop path remain within the wider portion.

The 99.3476 m² enclosed footprint, tandem car/camper layout, 90° rotation, 37° roof, room geometry and fixture refs are unchanged. The main house, carport and original landscape are unchanged. This relocation does not change the existing agricultural zoning restriction or concept status.

Source: actual **http://localhost:5173/** user profile, working v2 r59 against published r53, captured on 2026-09-10 in ignored `tmp/outbuilding-wide-site-1789067814444/workspace.json`. The previous canonical is preserved as `before-wide-site-outbuilding-r53.json`. A three-way ref/field merge found no conflicts; only the five already documented floating-point differences and revision metadata separated the source working copy from canonical r53. All earlier baselines remain unchanged. The r54 edit is a translation of r53 by (0, −10) m for the building and its outdoor entities, with the apron end reset to the retained entrance at z=74 m; historical generators continue to reproduce their original revisions.

Six outbuilding tests passed, including margin before the site narrowing, retained internal geometry, entrance connection, r53 migration with independent edits/deletions and conflict preservation. Code baseline remains `6d972d4` plus local changes. No commit, push or deployment was requested.

The actual localhost working copy is r60 after migration and matches canonical r54 apart from the preserved roundoff values and revision/date metadata. Its original project workspace is unchanged. Before/after exports and verification remain in the recovery directory. Fresh and existing isolated Chrome audits passed, including r53 migration and deletion survival across reload; results are in `output/wide-site-outbuilding-audit/`.

## Revision 55 — remove the two marked courtyard surfaces

The user's screenshot `Zrzut ekranu 2026-09-10 212129.png` identifies the stone-textured `zone/rain-garden` and yellow-green `zone/lawn`. Both zone entities are deleted. The nearby raised beds/crops, all buildings, roofs, terrace, fixtures and remaining zones are unchanged; the ordinary terrain shows through where those two overlays were removed.

Actual source: **http://localhost:5173/**, working v2 r60 against published r54, captured on 2026-09-10 in ignored `tmp/remove-marked-zones-1789068240912/workspace.json`. Previous canonical data are preserved in `before-zone-removal-r54.json`; immutable baselines remain unchanged. Three-way merge by ref/field found no conflicts, retaining the same five pre-existing roundoff differences in the browser. Schema/geometry validation passed. Canonical data revision is r55, code baseline remains `6d972d4` plus previously existing working changes; no application code, commit, push or deployment is part of this edit.

After browser reload/open, the visible v2 model and IndexedDB workspace are both r61 with neither deleted zone present. The original project workspace is unchanged. The after-export and comparison in the same recovery directory confirm only the established five roundoff differences versus canonical r55. A direct canonical comparison confirms exactly the two requested deletions plus revision/date metadata; no other project fields changed.

## Revision 64 — shorter hall and house, aligned bathroom/office wall

The user requested movement toward the kitchen/living/dining area, a single straight bathroom/office front, and then specified approximately **1.5 m** with a reduced outline. The main entrance, bathroom, utility partition and utility door move −1.5 m along house-local Z. The office front moves −1.685 m to remove the previous 18.5 cm step; both front walls now lie at Z=1.915 m. Their combined line is 7.58 m long, with the existing door openings retained. The obsolete return `wall/carport-layout/ground/6` is deleted from walls, floor membership and room boundaries. Connected ground-floor walls retain right angles.

The closing end walls move from Z=8.385 to 6.885 m on both storeys. Both slabs shorten by 1.5 m, reducing each gross footprint by **11.67 m²**; overall slab depth changes from 15.14 to 13.64 m. The two upstairs end bedrooms become shorter while their shared front partition stays fixed. Ground-floor bathroom and utility areas remain unchanged, and the office gains only the 18.5 cm alignment correction. Private-room furniture moves with the rooms. The entrance swing required the under-stair cabinet to move 15 cm toward the stairs. Day-area glazing, stairs, furnishings other than that cabinet, finishes and stable entity refs are preserved.

The long main gable ends at the new wall; its ridge direction and width stay fixed. Both main slopes remain **40.13423424862029°**, eaves 4.85 m and attic knee walls 1.40 m. Slab openings and living-room void remain unchanged. The independent road carport, side canopy/terrace, paths, landscape and outbuilding retain their existing geometry; shortening the enclosed house leaves a 1.5 m gap to the road carport. Main-house position and rotation are unchanged.

Source: actual **http://localhost:5173/** Codex in-app browser, captured on **2026-09-10**, working v2 **r63**, named by the user **“Garaz przód”**, against published r55. Ignored recovery directory: `tmp/short-hall-1_5m-1789075607524/`, containing before/after IndexedDB exports, live project, drafts, comparison plans and validation. `before-short-hall-r55.json` preserves the previous canonical; `before-short-hall-working-r63.json` preserves the extracted user layout. All immutable first-publication baselines remain unchanged. The first structured CDP export rounded four numbers at about 10⁻¹⁵; a ref/field three-way merge against the live r63 retained their full working precision. Later exports return JSON text from the browser to preserve those digits.

The requested edit was applied to the checked live r63 model and saved through the app persistence service, preserving proposals/drafts and adding an undo snapshot before reload. Canonical data then captured the applied **r64** rather than replacing the browser from r55. Published synchronization completed with **no conflicts**. An early HMR synchronization produced a temporary published copy before the edit was applied; that exact, unedited copy was backed up in recovery and removed after reconciliation. Final key comparison confirms no added project identities and no changes to other project/baseline records. After reload, the live project, saved workspace and canonical JSON match exactly at r64.

Reproduce the geometry with `python -m scripts.shorten-hall-study --source project-data/zielonki-v2/before-short-hall-working-r63.json --output tmp/short-hall-reproduction.json --shift-m 1.5 --shorten-shell`. This only writes a new draft; revision metadata is assigned when applying. Omitting `--shorten-shell` retains the previous fixed-envelope study behavior.

Validation: schema/geometry passes with the same 15 pre-existing warnings and no new warnings. Five dedicated tests cover aligned geometry, openings/furniture, roof constraints, fresh/existing migration, deletion persistence and preservation of conflicting edits. The suite passed 387 tests; the remaining grass-scattering test timed out under concurrent load and passed its isolated rerun (388 tests verified). Both fresh/existing browser tests pass, including reload, retained room rename and car deletion. TypeScript and production build pass with the existing Node/Vite compatibility and bundle warnings. Code baseline: `6d972d404d3dfe187c07b80b287c5cf9d19f37ed` plus working changes; data revision: **64**. No commit, merge, push or external deployment. Visual evidence: `output/short-hall/interior.jpg`, `ground-after.svg`, `upper-after.svg` and `tests/short-hall.spec.ts` screenshots.

## Revision 65 — left facade narrowed by 1.5 m, storage doorway removed

The user explicitly selected **1.5 m** inward movement of the whole left wall and removal of the **internal bathroom/storage door**. All segments of that facade move from local X=-5.495 to -3.995 m on both storeys. The connected ground-floor partitions shorten while retaining the aligned bathroom/office front at Z=1.915 m and right angles. `opening/reference-pantry` is deleted from the retained bathroom/storage partition. The external storage door remains and opens outward to clear the retained 2 m storage cabinet. The approach's inner edge extends to the relocated facade so both external doors still meet the path.

Ground-floor slab width changes from 11.19 to **9.69 m**; depth stays 13.64 m. Each slab loses **20.46 m²** of gross footprint. Clear bathroom dimensions are approximately 2.17 × 1.96 m (**4.24 m²**); storage 2.17 × 2.61 m (**5.65 m²**). Kitchen counters, island, stools, under-stair cabinet, WC and washer move with the facade. The storage cabinet moves 0.75 m to fit between its walls. Shower, vanity, guest bedroom/study, sofa and dining positions remain fixed.

The stairs retain their run, width and step count and move 1.5 m with their slab opening. The upper parents' bedroom and living-void edge move by the same amount, preserving the bed arrangement and 0.63 m separation between slab openings. Other upper internal partitions move 0.75 m to distribute the reduced width while retaining the room arrangement. Both end windows are repositioned, with their dimensions retained, to stay within their host walls and below the narrower gable. Both main roof pitches remain **40.13423424862029°**, eaves remain 4.85 m and knee walls 1.40 m. The narrowed long gable's symmetric ridge lowers by 0.75 × tan(pitch), approximately 0.632 m. The other main gable, canopy, carport, outbuilding and other landscape entities remain unchanged.

Source: actual **http://localhost:5173/** in-app browser, saved and live **r64**, captured on **2026-09-10** in ignored `tmp/narrow-house-1789076478153/workspace-before.json`. The export returns JSON text to preserve full numeric precision. Previous canonical is retained as `before-narrow-house-r64.json`; all immutable migration baselines remain unchanged. The checked live model received the explicit edit with an undo snapshot and preserved proposal/draft history, then was saved through the app persistence service. Canonical JSON captures applied **r65**. An initial CDP response timed out, but a subsequent read confirmed the save; the edit was not applied twice. After reload, live model, saved workspace and canonical r65 match exactly, published synchronization reports no conflicts, and every other existing project/baseline record is unchanged.

Reproduce the geometry with `python -m scripts.narrow-house-study --source project-data/zielonki-v2/before-narrow-house-r64.json --output tmp/narrow-house-reproduction.json --shift-m 1.5`. The script only creates a new draft, leaving revision assignment to application. Its small explicit coordinate groups preserve the existing layout and avoid changing unrelated model fields.

Validation: **393 unit tests pass**, including five new geometry, doorway, stairs, roof and migration/deletion regressions. Fresh and existing isolated-browser tests pass across reload while retaining an independent room rename and car deletion. Schema/geometry validation has the same 15 pre-existing warnings and no new errors or warnings. TypeScript and production build are checked separately. Actual-profile evidence is in `output/narrow-house/interior.jpg`, `ground.svg` and `upper.svg`. Code baseline remains `6d972d4` plus working changes; data revision is **65**. No commit, push, branch merge or deployment. The stopped local Vite preview was restarted on port 5173 using the bundled Node runtime; recovery logs remain ignored.

## Revision 66 — bathroom/storage gain 50 cm, guest-room doorway moves right

The user requested 0.5 m more bathroom and storage width at the expense of the guest room, with its doorway moved right to make room for a sofa. The connected ground-floor divider moves from X=-1.63 to **-1.13 m** over Z=1.915–6.885 m. Adjacent horizontal walls meet its new endpoints, preserving the straight front and all right angles. Clear bathroom area becomes approximately **5.22 m²**, storage **6.96 m²**, and guest room **14.38 m²**. The bathroom doorway retains its world position; the previously removed bathroom/storage door stays deleted.

The existing 0.90 m guest doorway moves 1.385 m to the right: center X=1.485 m, offset 0.60 m from the right-hand wall start. Its opening leaves 2.065 m of clear front wall to the left. A **2.00 × 0.90 m sofa envelope** centered at (0, 2.52) m fits inside the room and clears the doorway, bed and desk. This envelope is a placement check, not a new furniture entity. The desk rotates 90° and moves to (1.675, 3.8) m against the side wall to keep the entrance usable. Shower and vanity move 0.5 m with the divider. The bed and remaining furniture positions are retained. Upper-floor walls, stairs, slabs, exterior footprint, roof geometry/pitches, other buildings and landscape remain unchanged.

Source: actual **http://localhost:5173/** in-app browser, live and saved **r65**, captured as exact JSON text on **2026-09-10** in ignored `tmp/wider-bath-storage-1789077277707/workspace-before.json`. Previous canonical is preserved as `before-wider-bath-storage-r65.json`; immutable migration baselines remain unchanged. The explicit edit was applied only after exact comparison with the captured live model, with an undo snapshot and existing proposal/draft history preserved. Application persistence saved r66, which was then captured into canonical JSON. Recovery contains the source, candidate, validation and applied workspace. Entity refs are preserved, including the relocated doorway and desk; no separate project is created.

Validation: **396 unit tests pass**, including enlargement, sofa/door clearance and migration/deletion persistence. The project retains the same pre-existing warnings, with no new errors or warnings; the temporary sofa envelope has no placement warnings. Fresh/existing browser reload checks and TypeScript cover the new canonical data. Historical r65 narrowing tests now use the preserved r65 snapshot. Evidence: `output/wider-bath-storage/interior.jpg` and `plan.svg`. Code baseline remains `6d972d4` plus working changes; data revision is **66**. No commit, push, branch merge or external deployment.

Final r66 verification: actual-origin reload confirms exact equality of visible, saved and canonical projects, no synchronization conflicts and no modifications to other project/baseline records. Both fresh/existing browser tests and TypeScript pass. The documentation pass updated both project provenance READMEs; the test changes remain limited to this geometry and the preserved historical fixture.

## September 11 release — aligned main roof ridges, r67

The preserved before-roof-join-r66.json snapshot precedes this change. Both main roof segments meet at elevation 7.7856938754 m, with 4.85 m eaves and slopes of 44° and 37.87103769668754°. The 1.40 m knee walls remain. The corresponding rear-carport project is independently r88, with its previous r86 snapshot preserved. Regression tests check roof joins and retained geometry.
